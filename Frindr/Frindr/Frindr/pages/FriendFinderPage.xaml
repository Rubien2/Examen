<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Frindr.FriendFinderPage"
             Title="Vrienden zoeker">

    <StackLayout>
        <Frame>
            <StackLayout>
                <StackLayout Orientation="Horizontal">
                    <Button Text="Show filters" x:Name="ShowFilterButton" Clicked="ShowFilterButton_Clicked"></Button>
                </StackLayout>
                <StackLayout IsVisible="False" x:Name="ExtraFilterStackLayout">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Label x:Name="lblSelectedDistance" Grid.Column="0" Grid.Row="0" Text="Afstand:"></Label>
                        <Slider x:Name="DistanceSlider" Unfocused="DistanceSlider_Unfocused" ValueChanged="DistanceSlider_ValueChanged" 
                                Minimum="0" Maximum="150" HorizontalOptions="Fill"
                                MaximumTrackColor="Black" MinimumTrackColor="Black" 
                                Grid.Column="1" Grid.Row="0"/>
                    </Grid>
                    <Picker x:Name="AgePicker" SelectedIndexChanged="AgePicker_SelectedIndexChanged"></Picker>
                    </StackLayout>
            </StackLayout>
        </Frame>

        <ListView x:Name="FriendFinderListView"
                CachingStrategy="RecycleElement"
                ItemTapped="FriendFinderListView_ItemTapped"
                SelectedItem="{Binding id}"
                >
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="3*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Image Grid.Column="0" Grid.RowSpan="2" Grid.Row="0" BackgroundColor="Accent"></Image>
                            <Label Text="{Binding name}" VerticalTextAlignment="Center" Grid.Row="0" Grid.Column="1"/>
                            <Label Text="{Binding location}" Grid.Row="1" Grid.Column="1"></Label>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

    </StackLayout>

</ContentPage>